{
  "name": "nanollm",
  "version": "0.0.2",
  "description": "Express server that proxies llm request",
  "main": "./dist/server.js",
  "bin": {
    "nanollm": "dist/cli.js"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "node -e \"require('node:fs').rmSync('dist', { recursive: true, force: true })\" && tsc -p tsconfig.dist.json",
    "prepack": "npm run build",
    "start": "tsx server.ts",
    "dev": "tsx --watch server.ts",
    "typecheck": "tsc --noEmit",
    "test": "tsc -p tsconfig.build.json && node .test-dist/tests/run.js",
    "converter:test": "tsc -p tsconfig.build.json && node .test-dist/src/converters/test.js"
  },
  "keywords": [
    "llm",
    "proxy",
    "express",
    "litellm"
  ],
  "author": "sunwu51",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "@anthropic-ai/sdk": "^0.80.0",
    "dotenv": "^17.3.1",
    "express": "^5.2.1",
    "https-proxy-agent": "^8.0.0",
    "openai": "^6.32.0",
    "undici": "^7.24.5",
    "yaml": "^2.8.3"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^25.5.0",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  }
}
